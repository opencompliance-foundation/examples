# ExampleCo Signing Pack

This directory is a public-safe signing example for OpenCompliance.

It includes:

- a synthetic Ed25519 public key,
- a signed manifest for the issued ExampleCo corridor,
- and a signed manifest for the lifecycle artifacts.

The signer is synthetic and exists only to exercise the trust format. It is not a real production identity.
