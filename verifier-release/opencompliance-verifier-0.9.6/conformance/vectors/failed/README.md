# Failed Fixture Conformance Vector

This vector pins the expected outcome for the synthetic `failed` ExampleCo corridor.

The corridor exists to encode a failed machine-state claim as a first-class result instead of raising an exception.
